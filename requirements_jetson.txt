# ============================================================
# Jetson Orin Nano / Raspberry Pi — TinyML Dependencies
# ============================================================
# Zero PyTorch. Zero ultralytics. Minimal footprint.

# Image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── TinyML inference (pick ONE) ──────────────────────────────
# Option A: TFLite Runtime (~2 MB) — RECOMMENDED for ARM
tflite-runtime>=2.14.0

# Option B: ONNX Runtime (~50 MB) — fallback if no .tflite model
# onnxruntime>=1.15.0

# RapidOCR (PaddleOCR models via ONNX — still needed for OCR)
rapidocr-onnxruntime>=1.3.0

# MongoDB database (cloud persistence)
pymongo>=4.0.0
dnspython>=2.0.0

# Web server
flask>=2.0.0
requests>=2.28.0

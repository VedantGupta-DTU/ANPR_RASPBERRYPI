# ============================================================
# Jetson Orin Nano / Raspberry Pi — Minimal Dependencies
# ============================================================
# Zero PyTorch. Zero ultralytics. Just ONNX Runtime + OpenCV.

# Image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ONNX Runtime (YOLO detection + RapidOCR backend)
onnxruntime>=1.15.0

# RapidOCR (PaddleOCR models via ONNX Runtime — works on ARM64)
rapidocr-onnxruntime>=1.3.0

# MongoDB database (cloud persistence)
pymongo>=4.0.0
dnspython>=2.0.0

# Web server
flask>=2.0.0
requests>=2.28.0
